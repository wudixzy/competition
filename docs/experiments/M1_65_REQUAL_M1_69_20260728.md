# M1-65 requalification on the M1-69 runtime

Date: 2026-07-28

## Decision

The exact combined Q/K normalization candidate remains component-qualified on
the immutable M1-69 runtime. It is still default-off and still requires a
healthy TP4 service A/B.

This requalification does not authorize a `main` merge, formal YAML change,
default switch change, end-to-end performance claim, or model-capability
claim.

## Identity

The run used:

```text
instance: ssh-73ca29ba
physical GPU: 1
source: 37001edff643d98bf41bf4a52e0a145329003315
runtime: /root/m1-69-runtime-37001ed/site-packages
run root: /tmp/m1-69-m1-65-requal-37001ed-20260727T165256Z
```

The runtime identity gate passed before the benchmark. The runner used the
fixed production TP4 rank-local decode shape:

```text
batch=1, key_heads=4, value_heads=8, head_dim=128, dtype=FP16
```

It loaded no model weights and started no API service.

## Results

| Gate | Result |
| --- | ---: |
| Fixed seeds | 2/2 bit-exact |
| Sequence | 500/500 finite and bit-exact |
| Maximum normalized relative L2 | 0 |
| Maximum mapped relative L2 | 0 |
| Reference median | 0.109317 ms |
| Candidate median | 0.058323 ms |
| Component speedup | 1.87436x |
| Saving per layer | 0.050995 ms |
| Projected 30-layer saving | 1.52985 ms/token |

The fixed gate requires relative L2 at most `1e-5`, speedup at least `1.25x`,
saving of at least `0.02 ms` per layer, and 500 finite sequence steps. Every
gate passed without changing a threshold or scanning parameters.

The prior M1-65 run measured `1.8805x`; the current `1.87436x` result is
consistent with that component result on the newer overlay.

## Cleanup

The benchmark ran in its own process group. Cleanup sent SIGTERM with a
60-second grace, reserved SIGKILL for survivors, and reaped the child. It did
not use a broad process kill.

GPU1 passed before and after with:

```text
free bytes: 34,057,748,480
checksum: 1,073,741,824
free-memory drop: 0
```

Postflight observed three consecutive clean samples with no API server,
worker, or GPU process. Fatal and timeout scans were empty. Every runner gate
returned zero.

## Evidence

Structured evidence is under
`docs/experiments/evidence/M1_65_REQUAL_M1_69_20260728`. `SHA256SUMS` and
`tests/test_m1_65_requal_m1_69_evidence_unit.py` bind the exact benchmark,
qualification, runtime identity, GPU comparison, postflight, runner status,
and empty fatal/timeout scans.

The evidence contains no model output, prompt, response, model weight,
credential, or environment dump.

## Next gate

Retain the M1-65 candidate for the already defined service A/B:

```text
control:   strict-reference
candidate: strict-reference-combined-qk
```

Only healthy TP4 evidence can establish exact generated-output parity and
end-to-end benefit. Until then, keep
`BI100_GDN_COMBINED_QK_NORM=0` by default.
